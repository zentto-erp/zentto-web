# OSV-scanner ignore config — modular-frontend
# Vulns aceptadas con riesgo evaluado en SECURITY-EXCEPTIONS.md.
# Cualquier nueva vuln NO listada aquí bloqueará el gate.
# Re-evaluar cada entrada en cada sprint de seguridad.

[[IgnoredVulns]]
id = "GHSA-r5fr-rjxr-66jc"  # lodash@4.17.21 prototype pollution
ignoreUntil = 2027-04-25
reason = "Sin fix upstream. Funciones afectadas (lodash.merge/set/template con user input) NO se usan en el codebase. Verificado: solo se usan utilidades estándar (debounce, get, isEmpty)."

[[IgnoredVulns]]
id = "GHSA-h25m-26qc-wcjf"  # next@14.2.35 SSRF
ignoreUntil = 2027-04-25
reason = "next@14.2.35 viene como transitive de @zentto/landing-kit@2.1.0-beta.4. La vuln solo afecta runtime SSR de esa copia interna que NO se usa (landing-kit consume Next 16 del shell). Fix bloqueado por publish de nueva versión de @zentto/landing-kit con next 16."

[[IgnoredVulns]]
id = "GHSA-q4gf-8mx6-v5v3"  # next@14.2.35 race condition
ignoreUntil = 2027-04-25
reason = "Mismo path que GHSA-h25m-26qc-wcjf — next@14 transitive de landing-kit que no ejecuta runtime."
